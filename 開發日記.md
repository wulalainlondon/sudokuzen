# 數獨禪 (SudokuZen) 開發日記

> 記錄從 2026 年 2 月 1 日到 5 月 6 日，95 天、335 個 commit 的開發歷程。

---

## 序：為什麼重建

這個專案不是從零開始的。有一個舊版本，但它走到了盡頭。
2 月 1 日，我打出了「initial commit: complete rebuild of sudoku webapp」——一句話，把過去清掉了。

重建，不是因為失敗，而是因為知道自己想要什麼了。

---

## 第一章：地基（2026/2/1 — 2/15）

### Day 1：一口氣打出骨架

第一天的 7 個 commit 就能看出我急著把腦袋裡的藍圖釋放出來：

- 先讓遊戲能跑（initial commit）
- 立刻加 GitHub Pages 部署（trigger deployment）
- Service Worker 更新提示——玩家用的是 PWA，版本控制要從第一天就做對
- 重構 header / info bar / grid 分離——不想讓 CSS 變成一坨義大利麵
- 加 30 關初心者關卡

下午加了 Pause 功能，晚上加了主題切換。
這不是「邊想邊做」，這是把已經想好的東西逐條實現。

### 第一個讓我頭痛的問題：iPhone 14 Pro

```
fix(css): optimize numpad layout for small screens (iPhone 14 Pro) to prevent number 9 cutoff
fix(css): adjust grid max-height to 55dvh to fix squashing issue on iphone 14 pro
```

遊戲在手機上玩，所以手機顯示是 P0。
iPhone 14 Pro 的劉海會吃掉部分畫面，數字鍵盤的「9」被切掉了。
這兩行 fix 告訴我一件事：你不能假設螢幕是矩形的。

### 核心機制一次落地

接下來幾天，我把我認為一款好的數獨遊戲該有的東西全部補齊：

- **同數字高亮**：選一格，全盤同數字都亮起——視覺輔助，降低錯誤率
- **自動存檔 + 繼續進度**：關掉頁面再開，遊戲在同一格等你
- **星星評分系統**：完成有星，速度決定幾顆——簡單但有效的重玩動機
- **Firebase 排行榜**：第一通關時間上榜，競爭從這裡開始

### 生成「最難的 40 關」

2 月 2 日有一個特別的 commit：

```
feat: update Nirvana levels with new deterministic logic and add generation scripts
```

「寂滅」分頁是最高難度。那時我就決定：最難的關卡不能是隨便放進去的，要有科學依據。
我開始寫關卡生成 pipeline：挖洞、驗唯一解、評分。

後來這套 pipeline 越長越大。

### Firebase key 洩漏事件

```
fix: Remove leaked Firebase key and bump app version
feat: Add local Firebase override and ignore secrets
```

我把 Firebase API key commit 進去了。雖然 Firestore Security Rules 會擋大部分濫用，
但 key 公開放著終究是不對的。立刻修掉、加到 `.gitignore`、補上本地 override 機制。
往後每次涉及 API key 的地方都更小心了。

---

## 第二章：沉潛期（2026/2/15 — 3/9）

這段時間有整整三週，Git 是安靜的。

我沒有停工——我在想。
想這個遊戲到底要往哪裡走。
一款「只是做題」的數獨跟其他 App Store 上幾百個數獨 App 有什麼不同？

答案在 3 月 9 日的一波 commit 裡揭曉。

---

## 第三章：差異化的起點（2026/3/9）

3 月 9 日，我一口氣推了 10 個 commit，每一個都是在做別人沒做的東西。

### 隨機只挑「你還沒玩過的」

```
feat: filter random pool to only show unplayed levels
```

大部分數獨 App 的「隨機」是真隨機。玩家可能一直看到同一關。
我的隨機：只從尚未通關的關卡裡挑。這是對玩家時間的尊重。

### 答錯的 X 要「消失」

```
feat: show disappearing X instead of wrong number on error
fix: remove cross symbol completely from lives UI on error
```

答錯時生命值 ✖ 直接消失，而不是變色。
視覺語言要準確：消失 = 失去，不是「壞掉了但還在」。

### 競速模式

```
feat: add speedrun mode with delayed validation
```

在競速模式下，填滿整個盤面才一次驗證，答錯清除最後一格。
這是給老玩家的——你可以不用筆記，憑直覺跑。

### 回放可視化

```
feat: add visual replay board with play/pause/step/speed controls
```

通關後能「回放」自己怎麼解的，有進度條、有加速。
這是第一個讓我覺得「這個不一樣」的功能。

### 顯示這關需要什麼技巧

```
feat: show highest required skill in pre-level modal
```

進入關卡前先告訴你「這關需要 X-Wing 技巧」。
這個決策暗示了一件事：我打算讓玩家真的學習數獨解法，不只是猜猜猜。

### 第一個多人雛形：Ghost Racing

```
feat: implement ghost racing async multiplayer mode
```

異步對戰——你打你的，對手的鬼魂（ghost）在旁邊跑。
不需要同時在線，但有競爭感。這是之後「雙人即時對戰」的前一步。

---

## 第四章：教學系統 + 雙人模式的種子（2026/3/21 — 3/26）

### 技巧教學 Modal

```
feat: add technique teaching modal and extract CSS to separate file
```

一個教你如何解 X-Wing 的彈窗。簡單，但這是一整個教學系統的起點。

### Duo Room 誕生

```
feat: add duo room, stats page, achievements, and audio feedback
```

真正的雙人即時對戰。同一關，兩個人同時解，誰先解完誰贏。
這個 commit 帶出了三個子系統：房間管理、統計頁面、成就。

### 關卡命名：仙俠風四字標題

3 月 23-24 日，密集的命名工作：

```
chore: rename all sub-level titles to unique four-char poetic names
feat: elevate level naming with stage-specific classical Chinese lexicons
refactor: redesign level titles with stage-specific classical Chinese imagery
chore: apply xianxia naming and restore origin/nirvana names for 本源寂滅
```

920 個關卡，每一個都有獨一無二的四字名稱，分層用不同風格：
- 初心者：質樸的佛家詞彙
- 禪到虛空：水墨山水意象
- 無我本源：玄學哲思
- 寂滅以上：仙俠境界

這不是在「填名字」，這是在建立世界觀。

### 大爆發：3 月 26 日

3 月 26 日推了將近 20 個 commit，這是整個專案中最密集的單日開發。

**架構重組**：
```
feat: modular architecture + teach system overhaul
fix: break circular imports that prevented game completion
```
舊架構有循環引用，遊戲通關流程直接炸了。趁這次機會重組成模組化架構。

**連續填入模式**：
```
feat: continuous fill mode + auto-eliminate peer notes
```
選中一個數字後，點哪格填哪格，不用每次重選。自動消除同行/列/宮的候選數字筆記。
這個功能讓熟練玩家的操作速度提高了三倍。

**Apple Arcade 等級的音效和動畫**：
```
feat: Apple Arcade-grade audio, animations and continuous fill polish
```
我不想做一個「玩得到就好」的 App，我想讓每一個格子被填入時都有正向回饋。

**Duo 冷卻懲罰機制**：
```
feat: duo mode — same-cell cooldown penalty after lives depleted
feat: duo cooldown — shrinking cell mask shows remaining lock time
```
雙人模式答錯後，那格會被鎖住一段時間，而且用視覺上縮小的遮罩顯示剩餘時間。
緊張感從這裡來。

**ESLint + Prettier + CI**：
```
chore: add ESLint + Prettier + CI quality gate
```
程式碼品質從這天開始受到機器監管，不只是我個人的審美。

---

## 第五章：技術深度（2026/3/27 — 3/31）

3 月 27 日是另一個爆炸性的日子，但方向不同：這次是往技術深度鑽。

### 技巧偵測引擎

```
feat: technique detection engine — 40 techniques, integrated with replay
feat: record candidate eliminations in replay + technique detection
```

Solver 不只解題，還要知道「是用哪個技巧解的」。
40 種技巧，從 Naked Single 到 AIC，全部能被辨識和標記。
這讓回放變得有意義：你可以看到「第 23 步用了 X-Wing 消去了這個候選數」。

### 回放評分系統

```
feat: replay scoring system — technique points, guess penalty, grades
feat: cinematic score reveal — animated, sound-synced, zen-aesthetic
```

通關後有評分：用了什麼技巧、猜了幾次、最終分數和等第。
分數揭曉的動畫是電影感的——配合音效，逐漸展開。

### 成就系統重設計

```
feat: redesign achievement system — behavior-driven, not filler
```

原本的成就是「通關 10 關」之類的數字刷刷樂。
重設計後：成就要能反映你真正做到了什麼——比如「第一次用 Swordfish 解題」。

### 全 40 個技巧的教學資料

```
feat: replace all 37 fake teach boards with real puzzle data
feat: complete all 40 technique teach data — practice + descriptions
feat: teach practice UX — error explanation, smart hints, retry
```

教學 modal 裡，每個技巧都有：
1. 真實的題目（不是 made-up 的示意圖）
2. 示範動畫（電影式自動播放）
3. 練習題（可以試著操作）
4. 錯誤時的解釋，不是只說「答錯了」

### 故事驅動的示範系統

```
feat: story-driven demo system with locked_candidates prototype
feat: demo redesigned with neuroscience-backed timing
```

每個技巧的示範用「戲劇化」的方式呈現：
先展示混亂狀態 → 聚焦到關鍵格 → 技巧邏輯展開 → 豁然開朗。
動畫時序參考了感知研究，讓眼睛有足夠的時間跟上。

### 9437 關的謎題資料庫

```
feat: 9437-level puzzle database with 3-mode architecture + lazy loading
```

原本幾百關，現在接近一萬關。分三個模式各有不同的題庫。
Lazy loading：不一次全部載入，按需分片。

---

## 第六章：世界模式誕生（2026/3/30 — 3/31）

```
feat: 數獨：世界 (World Mode) — complete system implementation
```

「世界模式」是整個遊戲最大的賭注。
一個 RPG 化的數獨：你有等級、有 EXP、有遭遇、有技能。

**核心設計**：
- 每一關是一場「遭遇戰」，敵人是一個特定技巧的化身
- 打倒它，你習得這個技巧
- 習得的技巧可以在之後的對戰中作為「技能」施放
- 新手區（Lv.1-20）：自由探索，慢慢解鎖技能
- 記憶碎片系統：新手區的特殊收集物，引導玩家認識世界觀

**技術挑戰**：
```
feat: rebalance EXP curve + level-scaled tier probabilities
feat: staggered technique unlocks + smooth newbie encounter curves
feat: memory fragment system for newbie zone (Lv.1-20)
```

要讓 EXP 曲線讓玩家「剛好在適當時間解鎖下一個技巧」，需要很多調參。
不能太快（太容易），也不能太慢（失去新鮮感）。

---

## 第七章：React 化大遷移（2026/4/1）

4 月 1 日，一口氣做了大量的 React 遷移工作。

**背景**：原本的 UI 是 vanilla JS 操作 DOM，隨著功能增加，狀態管理越來越難追。
React 的聲明式 UI 能讓「這個 modal 何時該顯示」的邏輯清晰很多。

遷移了哪些？
```
feat: migrate Replay, DuoResult, AchievementToast, Library, Mentor to React
feat: migrate Game Over, Stats Modal, Pre-level Modal to React
feat: RPG skill tree UI + practice lobby React migration + duo wins sync
```

**但舊的不能直接丟**：遊戲核心邏輯是 vanilla JS，不能一次重寫。
所以採用「橋接模式」：React 渲染 UI，透過 `window` 橋接層和 vanilla 遊戲邏輯溝通。

```
fix: TEACH_DATA not on window — React bridge couldn't find teach data
fix: expose TEACH_DATA/levels/midPool to window via inline script
docs: add architecture rule for window bridge pattern
```

這橋接層是個雷，踩了好幾次。
最後決定：橋接模式是刻意的技術債，記錄在架構文件裡，等有時間再徹底整合。

### i18n 基礎建設

```
feat: i18n infrastructure — centralized string registry for localization
refactor: replace ~300 hardcoded strings with t() i18n calls
```

為了之後的多語言準備。
把 300 個寫死的中文字串全部抽換成 `t('key')` 的格式。

### 稱號系統

```
feat: title (稱號) system — earn from achievements, display in World/Duo
```

完成特定成就可以獲得稱號，在世界模式和雙人模式中顯示。
「初學者」、「寂滅勇者」、「殘局獵人」……小東西，但讓玩家有身份認同感。

---

## 第八章：Duo 2.0——做認真的多人對戰（2026/4/3 — 4/16）

前期的雙人模式是「能跑」的狀態。4 月開始，要做「做好」的狀態。

### 去掉上帝物件

```
refactor: decouple god objects and introduce navigation bus
```

原本有幾個超大的全域物件，幾乎所有功能都掛在上面。
這讓測試幾乎不可能，也讓 bug 的影響範圍難以預測。
引入事件 bus，模組之間透過訂閱發布溝通，而不是互相直接呼叫。

### 獨立的雙人謎題池

```
feat(duo): independent puzzle pool — duo-T*.json shards
feat(duo): generate 1600 fresh exclusive puzzles with candidate metadata
```

雙人模式需要「難度合適且兩人都沒玩過」的題目。
生成了 1600 個全新題目，按技巧等級分片，雙人模式專用。

### 奪秒模式（Chess Clock）

```
feat(duo): add Chess Clock mode and Tier 0 difficulty
feat(duo): rename chessClock to 奪秒, add BETA badge
```

象棋鐘機制：輪到你填格子時計時，填完才換對方。
這讓雙人對戰多了一層「思考時間分配」的策略維度。

### 觀戰模式

```
feat(duo): add spectator mode for first-finishing player
```

你先解完了，但對手還在解——你可以看他的棋盤。
還可以對他施放「炸彈」（清除一格），增加干擾樂趣。

### 幾個讓我崩潰的 Duo Bug

**Bug 1：心跳被靜默拒絕**（PR #1，2026-04-26）

Firestore Security Rules 用 `is int` 驗證，但 JavaScript SDK 存的是 float64。
結果所有進度更新、心跳都被靜默拒絕。
症狀：對手進度條永遠顯示 0%，60 秒後被判定斷線。
修復：把 `is int` 改成 `is number`。一個字。但找到這個 bug 花了很長時間。

**Bug 2：`spec_*` 欄位從未寫入**（PR #3，2026-04-29）

`duoCreateRoom` 從未寫入 4 個 `spec_*` 欄位，而 Security Rules 要求這些欄位必須存在。
症狀：房間建立後立刻被 `PERMISSION_DENIED`。

**Bug 3：23 個 skill 檔案沒有 git add**（同 PR #3）

加了 23 個技巧的 import，但實際的 `.ts` 檔案忘記 `git add`。
GitHub Pages build 持續失敗，本地完全正常。
這種 bug 最難找：本地看不到，只有 CI 才會爆。

---

## 第九章：Server-Side 邏輯（2026/4/22）

雙人對戰越來越複雜，Client-side 同步越來越不可靠。
4 月 22 日，一次把核心邏輯搬到 Cloud Functions。

```
feat(duo): Route C Phase 0 — Cloud Functions skeleton
feat(duo): Route C Phase 1 — submitFinish/forfeit via Cloud Functions
feat(duo): Route C Phase 2 — duoWatchdog server-side cleanup (Cloud Scheduler)
feat(duo): Route C Phase 3+4 — room lifecycle via Cloud Functions
feat(duo): Route C Phase 5 — full lockdown (createRoom/joinRoom via CF)
feat(duo): Route C Phase 6 — throwBomb via Cloud Function
```

現在所有關鍵動作（建房、加入、完成、棄賽、炸彈）都是 Cloud Function 處理。
Client 只能讀，不能直接寫關鍵欄位。Cheat-proof，也讓邏輯可測試。

---

## 第十章：音效和氣氛（2026/4/13 — 4/14）

```
feat(bgm): add looping BGM for wild and duo modes
feat(sfx): replace synthesized audio with Kenney CC0 sound files
feat(settings): add audio settings modal with SFX and BGM controls
```

前幾個月的音效是程式合成的（WebAudio API）。
換成 Kenney 的 CC0 音效素材後，感覺立刻不同——有質感，有重量。
BGM 是循環播放的，有淡入淡出銜接，不會突然切斷。
玩家可以在設定裡獨立控制 SFX 和 BGM 音量。

---

## 第十一章：自動化一切（2026/4/2 — 5/6）

### CI 品質門

```
chore: add ESLint + Prettier + CI quality gate
fix: add bundle size verification to deploy workflow
feat: release check guardrail for version/data/live consistency
```

每次 push 都跑：TypeScript 型別檢查、Lint、格式化檢查、單元測試。
Merge 前還跑：版本號和線上版本是否一致、資料是否正確。

### 自動部署

```
ci: add firebase preview hosting deployment without affecting pages
ci: auto-deploy Firestore rules and Cloud Functions on push to main
```

之前 Firestore Rules 要手動部署，有幾次修了 bug 但忘記部署，上線版本用的還是舊 Rules。
現在 push main 自動觸發全套部署。

### Firebase Config 安全化

```
security: remove hardcoded firebase api keys
security: remove Firebase API key from repo, inject via GitHub Secret
```

API key 從程式碼裡完全移除，改由 GitHub Actions secret 在 build time 注入。

---

## 第十二章：技巧可視化——最後一塊拼圖（2026/4/6 — 4/26）

### 強連結探索器

```
feat(sl-panel): Strong Link Explorer V1 — pause toggle + cell-tap partner highlight
```

Strong Link（強連結）是進階解法的核心概念。
這個面板讓玩家暫停遊戲，點擊格子，看到它的強連結夥伴亮起。

### AIC 追蹤面板

```
feat(chain-trace): V3 Phase 1 — text-based AIC/X-Chain trace with Mode 3A validation
feat(chain-trace): Mode 3B — optional elimination reveal after 3A validation
feat(chain-trace): Phase 2C — dual-track solver comparison after reveal
feat(chain-map): wire Phase 2A SVG link-map panel end-to-end
```

AIC（Alternating Inference Chain）是最難的數獨技巧之一。
這個面板：
1. 以文字步驟追蹤鏈的推導
2. 讓玩家自己找消去點（答對再揭曉）
3. 用 SVG 圖形畫出鏈的結構

### 20 種進階技巧完整實作

```
feat(skills): 完整實作 20 種高階技巧 + AIC 可視化 + Wild 首次遭遇隱藏
feat(skills): commit 23 skill implementations missing from dac8e62
```

ALS、Exocet、Death Blossom、Finned Jellyfish……
這些是「人類能理解」的最複雜解法，全部可以在遊戲中作為技能施放。

---

## 尾聲：95 天的座標

```
v2026.05.05-V1 — 目前版本
335 commits
7 PRs merged
0 open issues
```

**這個遊戲最後成了什麼？**

從一個「精緻的數獨 App」，長成了一個「學習數獨的完整平台」。

- **9437 個關卡**，從初心者到寂滅
- **41 種技巧**，每一種都有教學、示範、練習、和解法視覺化
- **三種主模式**：一般、世界（RPG）、修行（技巧練習樹）
- **雙人即時對戰**，含奪秒、觀戰、炸彈
- **完整的技巧偵測和回放評分**

最讓我在意的不是哪個功能，而是一個信念：
數獨不是在猜，是在推理。
這個遊戲想讓更多人感受到——找到邏輯突破口的那一刻，是真實的智識快感。

---

*最後更新：2026-05-07*
