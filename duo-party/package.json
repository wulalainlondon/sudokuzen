{
  "name": "duo-party",
  "private": true,
  "type": "module",
  "description": "Phase 0 spike: Cloudflare Durable Objects (partyserver) latency probe for Duo realtime migration",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check:hibernation": "node scripts/assert-hibernation.mjs",
    "qa:phase3": "node scripts/run-phase3-qa.mjs",
    "check": "npm run typecheck && npm run check:hibernation && wrangler deploy --dry-run && npm run qa:phase3",
    "latency": "node src/latency-test.mjs"
  },
  "dependencies": {
    "partyserver": "^0.5.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "wrangler": "^4.118.0",
    "ws": "latest"
  }
}
