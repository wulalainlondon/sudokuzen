{
  "name": "duo-party",
  "private": true,
  "type": "module",
  "description": "Phase 0 spike: Cloudflare Durable Objects (partyserver) latency probe for Duo realtime migration",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "qa:phase3": "node scripts/run-phase3-qa.mjs",
    "check": "npm run typecheck && wrangler deploy --dry-run && npm run qa:phase3",
    "latency": "node src/latency-test.mjs"
  },
  "dependencies": {
    "partyserver": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "ws": "latest",
    "wrangler": "latest"
  }
}
